export type PaginationPropsType = {
  limit: number,
  nextPage: boolean,
  page: number,
  previousPage: boolean,
  totalPages: number,
  totalItems: number,
  currentPageItems: number,
  modelValue: number
}
